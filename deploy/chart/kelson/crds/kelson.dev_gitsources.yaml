# Code generated by internal/schemagen. DO NOT EDIT.
#
# The openAPIV3Schema below is reflected from the Go types in internal/model —
# the same reflection that produces schema/*.json — so the CRD and the published
# JSON Schema cannot disagree about what a valid kelson spec is (ADR-0027
# decision 4). Regenerate with:
#
#	go generate ./internal/model
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: gitsources.kelson.dev
  labels:
    app.kubernetes.io/name: kelson
    app.kubernetes.io/component: crd
spec:
  group: kelson.dev
  names:
    kind: GitSource
    listKind: GitSourceList
    plural: gitsources
    singular: gitsource
    shortNames:
      - ksrc
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Repository
          type: string
          jsonPath: .spec.git
          description: the repository this source offers
        - name: Ref
          type: string
          jsonPath: .spec.ref
          description: the branch, tag or commit it is read at
        - name: Connection
          type: string
          jsonPath: .spec.connection
          description: the GitConnection it is read with; empty means the host match
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
          description: whether the document validated
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          description: 'GitSource is a repository the instance offers to every project: where the code is, at which ref, through which connection, and who owns it. Its name is what a component binds to with `source: <name>`, and a project declaring a source of that name shadows it (ADR-0035).'
          type: object
          properties:
            apiVersion:
              description: APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - git
              properties:
                connection:
                  description: name of the GitConnection to authenticate with; resolved by host match against the instance's connections when omitted
                  type: string
                git:
                  description: git URL of the repository this source offers
                  type: string
                  format: uri
                owner:
                  type: object
                  required:
                    - kind
                  properties:
                    kind:
                      description: who may edit this document; only instance is enforced today
                      type: string
                      enum:
                        - instance
                        - user
                        - team
                    name:
                      description: the owning principal; required when kind is user or team and refused when it is instance
                      type: string
                  x-kubernetes-validations:
                    - rule: '!has(self.kind) || self.kind == ''instance'' || (has(self.name) && self.name != '''')'
                      message: an owner whose kind is user or team names the principal it belongs to (schema/missing-required)
                    - rule: '!has(self.kind) || self.kind != ''instance'' || !has(self.name) || self.name == '''''
                      message: 'an instance-owned source names no principal: remove owner.name, or set owner.kind to user or team (schema/mutually-exclusive)'
                ref:
                  type: string
            status:
              description: 'GitSourceStatus is what the control plane observed about this GitSource: whether the document is usable, as of which generation. Reachability belongs to the GitConnection that serves it.'
              type: object
              properties:
                conditions:
                  description: the observed conditions, with Ready as the summary
                  type: array
                  items:
                    type: object
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        description: when the condition last changed from one status to another
                        type: string
                        format: date-time
                      message:
                        description: a human-readable message about the transition, which may be empty
                        type: string
                        maxLength: 32768
                      observedGeneration:
                        description: the .metadata.generation the condition was set from
                        type: integer
                        format: int64
                        minimum: 0
                      reason:
                        description: a programmatic identifier for the condition's last transition
                        type: string
                        minLength: 1
                        maxLength: 1024
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      status:
                        description: True, False or Unknown
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      type:
                        description: the condition's type, in CamelCase; Ready is kelson's summary condition
                        type: string
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys:
                    - type
                observedGeneration:
                  description: the .metadata.generation this status describes. A status whose observedGeneration trails the object's generation has not caught up yet.
                  type: integer
                  format: int64
                validationErrors:
                  description: 'what validate.go said about this document. An invalid spec is a status and not a rejection: nothing renders, nothing is published, and the previous revision keeps serving.'
                  type: array
                  items:
                    type: object
                    required:
                      - code
                      - message
                    properties:
                      code:
                        description: the stable, machine-actionable code, e.g. schema/unknown-field
                        type: string
                      column:
                        description: 1-based column in the authored YAML, when the source was available
                        type: integer
                      docsUrl:
                        description: documentation page for this code
                        type: string
                      field:
                        description: JSONPath-like location within the document, e.g. $.spec.components[2].port
                        type: string
                      line:
                        description: 1-based line in the authored YAML, when the source was available
                        type: integer
                      message:
                        description: what is wrong
                        type: string
                      remediation:
                        description: what to do about it, stated as an action
                        type: string
                      resource:
                        description: the document the error is about, e.g. Environment/production
                        type: string
          required:
            - spec
