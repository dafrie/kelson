// InstallService: the platform-component install surface of ADR-0021, served
// (issue #60). It is `kelson install` at the API plane: the same pins table,
// the same detection-first eligibility, the same preview-confirm-apply shape —
// the confirmation step is the client's (the UI shows the plan and asks), and
// Install re-plans server-side so nothing stale is ever applied.
//
// ListComponents is the onboarding question — "what does this cluster have,
// and what is kelson offering to add?" — answered from the pins table crossed
// with live detection. Presence is the tri-state of issue #144 and travels as
// data: "yes", "no" and "unknown" are three answers, and a client must never
// collapse the third into the second.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kelson/v1alpha1/install.proto

package kelsonv1alpha1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1alpha1 "github.com/dafrie/kelson/internal/api/gen/kelson/v1alpha1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// InstallServiceName is the fully-qualified name of the InstallService service.
	InstallServiceName = "kelson.v1alpha1.InstallService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// InstallServiceListComponentsProcedure is the fully-qualified name of the InstallService's
	// ListComponents RPC.
	InstallServiceListComponentsProcedure = "/kelson.v1alpha1.InstallService/ListComponents"
	// InstallServicePlanInstallProcedure is the fully-qualified name of the InstallService's
	// PlanInstall RPC.
	InstallServicePlanInstallProcedure = "/kelson.v1alpha1.InstallService/PlanInstall"
	// InstallServiceInstallProcedure is the fully-qualified name of the InstallService's Install RPC.
	InstallServiceInstallProcedure = "/kelson.v1alpha1.InstallService/Install"
)

// InstallServiceClient is a client for the kelson.v1alpha1.InstallService service.
type InstallServiceClient interface {
	// ListComponents reports every component kelson knows how to install (or
	// deliberately does not), with detection's presence verdict for each.
	ListComponents(context.Context, *connect.Request[v1alpha1.ListComponentsRequest]) (*connect.Response[v1alpha1.ListComponentsResponse], error)
	// PlanInstall computes what an install would do, without applying anything:
	// every object, the namespace it lands in, the pinned version and the
	// verified digest.
	PlanInstall(context.Context, *connect.Request[v1alpha1.PlanInstallRequest]) (*connect.Response[v1alpha1.PlanInstallResponse], error)
	// Install re-plans and applies. Cluster-scoped RBAC, CRDs and webhook
	// configurations are written; the scope table marks this administrative, so
	// agent credentials are refused outright (ADR-0024 §3).
	Install(context.Context, *connect.Request[v1alpha1.InstallRequest]) (*connect.Response[v1alpha1.InstallResponse], error)
}

// NewInstallServiceClient constructs a client for the kelson.v1alpha1.InstallService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewInstallServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) InstallServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	installServiceMethods := v1alpha1.File_kelson_v1alpha1_install_proto.Services().ByName("InstallService").Methods()
	return &installServiceClient{
		listComponents: connect.NewClient[v1alpha1.ListComponentsRequest, v1alpha1.ListComponentsResponse](
			httpClient,
			baseURL+InstallServiceListComponentsProcedure,
			connect.WithSchema(installServiceMethods.ByName("ListComponents")),
			connect.WithClientOptions(opts...),
		),
		planInstall: connect.NewClient[v1alpha1.PlanInstallRequest, v1alpha1.PlanInstallResponse](
			httpClient,
			baseURL+InstallServicePlanInstallProcedure,
			connect.WithSchema(installServiceMethods.ByName("PlanInstall")),
			connect.WithClientOptions(opts...),
		),
		install: connect.NewClient[v1alpha1.InstallRequest, v1alpha1.InstallResponse](
			httpClient,
			baseURL+InstallServiceInstallProcedure,
			connect.WithSchema(installServiceMethods.ByName("Install")),
			connect.WithClientOptions(opts...),
		),
	}
}

// installServiceClient implements InstallServiceClient.
type installServiceClient struct {
	listComponents *connect.Client[v1alpha1.ListComponentsRequest, v1alpha1.ListComponentsResponse]
	planInstall    *connect.Client[v1alpha1.PlanInstallRequest, v1alpha1.PlanInstallResponse]
	install        *connect.Client[v1alpha1.InstallRequest, v1alpha1.InstallResponse]
}

// ListComponents calls kelson.v1alpha1.InstallService.ListComponents.
func (c *installServiceClient) ListComponents(ctx context.Context, req *connect.Request[v1alpha1.ListComponentsRequest]) (*connect.Response[v1alpha1.ListComponentsResponse], error) {
	return c.listComponents.CallUnary(ctx, req)
}

// PlanInstall calls kelson.v1alpha1.InstallService.PlanInstall.
func (c *installServiceClient) PlanInstall(ctx context.Context, req *connect.Request[v1alpha1.PlanInstallRequest]) (*connect.Response[v1alpha1.PlanInstallResponse], error) {
	return c.planInstall.CallUnary(ctx, req)
}

// Install calls kelson.v1alpha1.InstallService.Install.
func (c *installServiceClient) Install(ctx context.Context, req *connect.Request[v1alpha1.InstallRequest]) (*connect.Response[v1alpha1.InstallResponse], error) {
	return c.install.CallUnary(ctx, req)
}

// InstallServiceHandler is an implementation of the kelson.v1alpha1.InstallService service.
type InstallServiceHandler interface {
	// ListComponents reports every component kelson knows how to install (or
	// deliberately does not), with detection's presence verdict for each.
	ListComponents(context.Context, *connect.Request[v1alpha1.ListComponentsRequest]) (*connect.Response[v1alpha1.ListComponentsResponse], error)
	// PlanInstall computes what an install would do, without applying anything:
	// every object, the namespace it lands in, the pinned version and the
	// verified digest.
	PlanInstall(context.Context, *connect.Request[v1alpha1.PlanInstallRequest]) (*connect.Response[v1alpha1.PlanInstallResponse], error)
	// Install re-plans and applies. Cluster-scoped RBAC, CRDs and webhook
	// configurations are written; the scope table marks this administrative, so
	// agent credentials are refused outright (ADR-0024 §3).
	Install(context.Context, *connect.Request[v1alpha1.InstallRequest]) (*connect.Response[v1alpha1.InstallResponse], error)
}

// NewInstallServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewInstallServiceHandler(svc InstallServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	installServiceMethods := v1alpha1.File_kelson_v1alpha1_install_proto.Services().ByName("InstallService").Methods()
	installServiceListComponentsHandler := connect.NewUnaryHandler(
		InstallServiceListComponentsProcedure,
		svc.ListComponents,
		connect.WithSchema(installServiceMethods.ByName("ListComponents")),
		connect.WithHandlerOptions(opts...),
	)
	installServicePlanInstallHandler := connect.NewUnaryHandler(
		InstallServicePlanInstallProcedure,
		svc.PlanInstall,
		connect.WithSchema(installServiceMethods.ByName("PlanInstall")),
		connect.WithHandlerOptions(opts...),
	)
	installServiceInstallHandler := connect.NewUnaryHandler(
		InstallServiceInstallProcedure,
		svc.Install,
		connect.WithSchema(installServiceMethods.ByName("Install")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kelson.v1alpha1.InstallService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case InstallServiceListComponentsProcedure:
			installServiceListComponentsHandler.ServeHTTP(w, r)
		case InstallServicePlanInstallProcedure:
			installServicePlanInstallHandler.ServeHTTP(w, r)
		case InstallServiceInstallProcedure:
			installServiceInstallHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedInstallServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedInstallServiceHandler struct{}

func (UnimplementedInstallServiceHandler) ListComponents(context.Context, *connect.Request[v1alpha1.ListComponentsRequest]) (*connect.Response[v1alpha1.ListComponentsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kelson.v1alpha1.InstallService.ListComponents is not implemented"))
}

func (UnimplementedInstallServiceHandler) PlanInstall(context.Context, *connect.Request[v1alpha1.PlanInstallRequest]) (*connect.Response[v1alpha1.PlanInstallResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kelson.v1alpha1.InstallService.PlanInstall is not implemented"))
}

func (UnimplementedInstallServiceHandler) Install(context.Context, *connect.Request[v1alpha1.InstallRequest]) (*connect.Response[v1alpha1.InstallResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kelson.v1alpha1.InstallService.Install is not implemented"))
}
