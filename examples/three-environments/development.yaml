apiVersion: kelson.dev/v1alpha1
kind: Environment
metadata:
  name: development

# Direct mode: kelson server-side applies. Rendered history keeps diffs
# and rollback even here (ADR-0001).
spec:
  project: billing

  routing:
    domainSuffix: dev.billing.internal

  delivery:
    mode: direct

  # `policy: {agents: allow}` would let agents deploy freely here. It returns
  # with M7; kelson rejects unimplemented fields rather than ignoring them (#141).
