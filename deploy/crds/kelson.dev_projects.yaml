# Code generated by internal/schemagen. DO NOT EDIT.
#
# The openAPIV3Schema below is reflected from the Go types in internal/model —
# the same reflection that produces schema/*.json — so the CRD and the published
# JSON Schema cannot disagree about what a valid kelson spec is (ADR-0027
# decision 4). Regenerate with:
#
#	go generate ./internal/model
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: projects.kelson.dev
  labels:
    app.kubernetes.io/name: kelson
    app.kubernetes.io/component: crd
spec:
  group: kelson.dev
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
    shortNames:
      - kproj
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
          description: whether the document validated
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          description: 'Project is the shared-configuration document: image and build coordinates, shared environment, and the components that deploy together. Everything that differs per deployment target lives in an Environment.'
          type: object
          properties:
            apiVersion:
              description: APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - components
              properties:
                build:
                  type: object
                  properties:
                    by:
                      description: who produces this project's images; kelson builds them in its own build plane and ci reports images its pipeline already built
                      type: string
                      enum:
                        - kelson
                        - ci
                    dockerfile:
                      type: string
                    strategy:
                      type: string
                      enum:
                        - auto
                        - dockerfile
                        - buildpacks
                        - none
                components:
                  type: array
                  minItems: 1
                  items:
                    type: object
                    required:
                      - name
                    properties:
                      auth:
                        description: valkey components only; names the Secret holding the cache password — kelson references it and never creates or reads it
                        type: object
                        required:
                          - secret
                          - key
                        properties:
                          key:
                            description: key within that Secret
                            type: string
                          secret:
                            description: name of a Secret in the environment's namespace; kelson references it and never creates or reads it
                            type: string
                      chart:
                        description: helm components only; the chart name within its source
                        type: string
                      chartVersion:
                        description: helm components only; the exact chart version — required because an unpinned chart is not reproducible
                        type: string
                      command:
                        description: container command; wins over the image default
                        type: array
                        items:
                          type: string
                      domains:
                        type: array
                        items:
                          type: string
                      env:
                        type: object
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                      health:
                        description: HTTP liveness/readiness path
                        type: string
                      image:
                        description: overrides the Project image (rule P3)
                        type: string
                      kind:
                        description: derived from port/schedule when omitted; required for postgres and valkey and helm
                        type: string
                        enum:
                          - service
                          - worker
                          - cron
                          - agent
                          - postgres
                          - valkey
                          - helm
                      name:
                        type: string
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      port:
                        type: integer
                        minimum: 1
                        maximum: 65535
                      preset:
                        description: data components only
                        type: string
                        enum:
                          - shared
                          - small
                          - ha-small
                          - ha-medium
                          - branch
                      release:
                        description: command run to completion before this revision's workloads roll — direct delivery mode only
                        type: object
                        required:
                          - command
                        properties:
                          command:
                            description: argv of the command; it runs with the component's image and environment
                            type: array
                            minItems: 1
                            items:
                              type: string
                          timeout:
                            description: Go duration such as 30m; the Job's activeDeadlineSeconds
                            type: string
                      replicas:
                        type: object
                        required:
                          - min
                        properties:
                          max:
                            type: integer
                            minimum: 0
                          min:
                            type: integer
                            minimum: 0
                        x-kubernetes-validations:
                          - rule: '!has(self.max) || self.max == 0 || !has(self.min) || self.max >= self.min'
                            message: replicas.max must be 0 (a fixed count of replicas.min) or at least replicas.min (schema/out-of-range)
                      resources:
                        type: object
                        properties:
                          limits:
                            type: object
                            properties:
                              cpu:
                                description: Kubernetes quantity
                                type: string
                              memory:
                                description: Kubernetes quantity
                                type: string
                          requests:
                            type: object
                            properties:
                              cpu:
                                description: Kubernetes quantity
                                type: string
                              memory:
                                description: Kubernetes quantity
                                type: string
                      schedule:
                        description: five-field cron expression
                        type: string
                      source:
                        description: a source name for a buildable component (ADR-0035) or the chart source of a helm component — exactly one of repository or oci (ADR-0016)
                        x-kubernetes-preserve-unknown-fields: true
                      tools:
                        description: 'agent components only; refused until issue #75'
                        type: array
                        items:
                          type: string
                      values:
                        description: helm components only; chart values rendered verbatim into the HelmRelease — plain configuration only and never secret material (put that in valuesFrom)
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      valuesFrom:
                        description: helm components only; Secrets and ConfigMaps merged into the chart values by helm-controller
                        type: array
                        items:
                          type: object
                          properties:
                            configMapRef:
                              description: name of a ConfigMap in the environment namespace
                              type: string
                            secretRef:
                              description: name of a Secret in the environment namespace
                              type: string
                    x-kubernetes-validations:
                      - rule: '!(has(self.port) && has(self.schedule))'
                        message: 'a component sets port or schedule and never both: a port makes it a service, a schedule makes it a cron (schema/mutually-exclusive)'
                defaults:
                  type: object
                  properties:
                    deliveryMode:
                      type: string
                    policy:
                      type: object
                      properties:
                        agents:
                          description: what an agent may do here unsupervised; propose-only refuses every live mutation
                          type: string
                          enum:
                            - allow
                            - propose-only
                        deployers:
                          type: array
                          items:
                            type: string
                        forbid:
                          description: agents only; operations refused to agents here — one of deploy / rollback / promote / build / secret-set / secret-delete / spec-write / spec-delete
                          type: array
                          items:
                            type: string
                        maxReplicas:
                          description: agents only; the highest replica count an agent may deploy in this environment
                          type: integer
                          minimum: 1
                        protect:
                          description: agents only; components an agent may not delete or scale to zero
                          type: array
                          items:
                            type: string
                        require:
                          description: guards that must hold before an agent deploy; only dry-run is defined
                          type: array
                          items:
                            type: string
                    secrets:
                      type: object
                      required:
                        - backend
                      properties:
                        ageKeySecret:
                          description: sops only; name of the Secret holding the age identity — created by the operator in the Kustomization's namespace
                          type: string
                        ageRecipients:
                          description: sops only; age public keys (age1…) that encrypted secrets are readable by — required for backend sops
                          type: array
                          items:
                            type: string
                        backend:
                          type: string
                          enum:
                            - cluster
                            - externalSecrets
                            - sops
                        refreshInterval:
                          description: externalSecrets only; how often the value is re-read from the backing store; a positive Go duration such as 30s or 15m or 1h
                          type: string
                        store:
                          description: externalSecrets only; name of a SecretStore in this namespace or a ClusterSecretStore — optional when the cluster offers exactly one
                          type: string
                env:
                  type: object
                  additionalProperties:
                    x-kubernetes-preserve-unknown-fields: true
                image:
                  description: pre-built image reference
                  type: string
                overlays:
                  type: array
                  items:
                    type: object
                    properties:
                      manifest:
                        description: path to an extra Kubernetes manifest
                        type: string
                      patch:
                        description: YAML document merged into the resource it targets
                        type: string
                source:
                  type: object
                  required:
                    - git
                  properties:
                    connection:
                      description: name of the GitConnection to authenticate with; resolved by host match against the instance's connections when omitted
                      type: string
                    git:
                      description: git URL of the application source
                      type: string
                      format: uri
                    name:
                      description: DNS-1123 label a component binds to; required in spec.sources and refused on the singular spec.source
                      type: string
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      maxLength: 63
                    ref:
                      type: string
                sources:
                  description: 'repositories this Project declares for its components to build from; the singular source: is shorthand for one entry named default'
                  type: array
                  items:
                    type: object
                    required:
                      - git
                    properties:
                      connection:
                        description: name of the GitConnection to authenticate with; resolved by host match against the instance's connections when omitted
                        type: string
                      git:
                        description: git URL of the application source
                        type: string
                        format: uri
                      name:
                        description: DNS-1123 label a component binds to; required in spec.sources and refused on the singular spec.source
                        type: string
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        maxLength: 63
                      ref:
                        type: string
            status:
              description: ProjectStatus is what the controller observed about this Project. A Project has no delivery of its own, so its status is validation and nothing else.
              type: object
              properties:
                conditions:
                  description: the observed conditions, with Ready as the summary
                  type: array
                  items:
                    type: object
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        description: when the condition last changed from one status to another
                        type: string
                        format: date-time
                      message:
                        description: a human-readable message about the transition, which may be empty
                        type: string
                        maxLength: 32768
                      observedGeneration:
                        description: the .metadata.generation the condition was set from
                        type: integer
                        format: int64
                        minimum: 0
                      reason:
                        description: a programmatic identifier for the condition's last transition
                        type: string
                        minLength: 1
                        maxLength: 1024
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      status:
                        description: True, False or Unknown
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      type:
                        description: the condition's type, in CamelCase; Ready is kelson's summary condition
                        type: string
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys:
                    - type
                observedGeneration:
                  description: the .metadata.generation this status describes. A status whose observedGeneration trails the object's generation has not caught up yet.
                  type: integer
                  format: int64
                validationErrors:
                  description: 'what validate.go said about this document. An invalid spec is a status and not a rejection: nothing renders, nothing is published, and the previous revision keeps serving.'
                  type: array
                  items:
                    type: object
                    required:
                      - code
                      - message
                    properties:
                      code:
                        description: the stable, machine-actionable code, e.g. schema/unknown-field
                        type: string
                      column:
                        description: 1-based column in the authored YAML, when the source was available
                        type: integer
                      docsUrl:
                        description: documentation page for this code
                        type: string
                      field:
                        description: JSONPath-like location within the document, e.g. $.spec.components[2].port
                        type: string
                      line:
                        description: 1-based line in the authored YAML, when the source was available
                        type: integer
                      message:
                        description: what is wrong
                        type: string
                      remediation:
                        description: what to do about it, stated as an action
                        type: string
                      resource:
                        description: the document the error is about, e.g. Environment/production
                        type: string
          required:
            - spec
