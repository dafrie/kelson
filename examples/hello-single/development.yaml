apiVersion: kelson.dev/v1alpha1
kind: Environment
metadata:
  name: development

spec:
  project: hello

  routing:
    domainSuffix: dev.acme.run

  # `policy: {agents: allow}` — development is where agents deploy freely —
  # returns with M7; kelson rejects unimplemented fields rather than ignoring
  # them (#141).
