# Code generated by internal/schemagen. DO NOT EDIT.
#
# The openAPIV3Schema below is reflected from the Go types in internal/model —
# the same reflection that produces schema/*.json — so the CRD and the published
# JSON Schema cannot disagree about what a valid kelson spec is (ADR-0027
# decision 4). Regenerate with:
#
#	go generate ./internal/model
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: environments.kelson.dev
  labels:
    app.kubernetes.io/name: kelson
    app.kubernetes.io/component: crd
spec:
  group: kelson.dev
  names:
    kind: Environment
    listKind: EnvironmentList
    plural: environments
    singular: environment
    shortNames:
      - kenv
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Phase
          type: string
          jsonPath: .status.phase
          description: the delivery state-machine phase
        - name: Revision
          type: string
          jsonPath: .status.revision
          description: the settled revision
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
          description: whether the document validated and rendered
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          description: 'Environment is where a Project runs and what differs there: target namespace, routing, policy, secret backend and per-component overrides. It binds to a Project in the same namespace by name (spec.project).'
          type: object
          properties:
            apiVersion:
              description: APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - project
              properties:
                autoDeploy:
                  description: follow the components' sources — a push to a bound repository re-renders and republishes this environment (ADR-0036)
                  type: boolean
                cluster:
                  type: string
                components:
                  type: array
                  items:
                    type: object
                    required:
                      - name
                    properties:
                      autoDeploy:
                        description: workloads only; follow this component's source here — it overrides the environment's own setting (ADR-0036)
                        type: boolean
                      env:
                        type: object
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                      image:
                        description: pins this component's image in this environment only; the promotion primitive (rule P3)
                        type: string
                      name:
                        type: string
                      preset:
                        description: data components only
                        type: string
                        enum:
                          - shared
                          - small
                          - ha-small
                          - ha-medium
                          - branch
                      replicas:
                        type: object
                        required:
                          - min
                        properties:
                          max:
                            type: integer
                            minimum: 0
                          min:
                            type: integer
                            minimum: 0
                        x-kubernetes-validations:
                          - rule: '!has(self.max) || self.max == 0 || !has(self.min) || self.max >= self.min'
                            message: replicas.max must be 0 (a fixed count of replicas.min) or at least replicas.min (schema/out-of-range)
                      resources:
                        type: object
                        properties:
                          limits:
                            type: object
                            properties:
                              cpu:
                                description: Kubernetes quantity
                                type: string
                              memory:
                                description: Kubernetes quantity
                                type: string
                          requests:
                            type: object
                            properties:
                              cpu:
                                description: Kubernetes quantity
                                type: string
                              memory:
                                description: Kubernetes quantity
                                type: string
                delivery:
                  type: object
                  required:
                    - mode
                  properties:
                    git:
                      type: object
                      required:
                        - repo
                      properties:
                        branch:
                          type: string
                        path:
                          description: directory within the repo for rendered manifests
                          type: string
                        repo:
                          description: git URL of the deployment repository
                          type: string
                    mode:
                      type: string
                      enum:
                        - direct
                        - flux
                namespace:
                  type: string
                overlays:
                  type: array
                  items:
                    type: object
                    properties:
                      manifest:
                        description: path to an extra Kubernetes manifest
                        type: string
                      patch:
                        description: YAML document merged into the resource it targets
                        type: string
                policy:
                  type: object
                  properties:
                    agents:
                      description: what an agent may do here unsupervised; propose-only refuses every live mutation
                      type: string
                      enum:
                        - allow
                        - propose-only
                    deployers:
                      type: array
                      items:
                        type: string
                    forbid:
                      description: agents only; operations refused to agents here — one of deploy / rollback / promote / build / secret-set / secret-delete / spec-write / spec-delete
                      type: array
                      items:
                        type: string
                    maxReplicas:
                      description: agents only; the highest replica count an agent may deploy in this environment
                      type: integer
                      minimum: 1
                    protect:
                      description: agents only; components an agent may not delete or scale to zero
                      type: array
                      items:
                        type: string
                    require:
                      description: guards that must hold before an agent deploy; only dry-run is defined
                      type: array
                      items:
                        type: string
                previews:
                  type: object
                  required:
                    - provider
                    - repo
                    - artifacts
                  properties:
                    artifacts:
                      type: object
                      required:
                        - repository
                      properties:
                        repository:
                          description: oci:// URL of the repository holding per-pull-request manifests; no tag
                          type: string
                        secretRef:
                          description: name of a docker-registry Secret for a private artifact repository
                          type: string
                    filter:
                      type: object
                      properties:
                        excludeBranch:
                          description: regular expression; matching branches are excluded
                          type: string
                        includeBranch:
                          description: regular expression; only matching branches become previews
                          type: string
                        labels:
                          description: only change requests carrying one of these labels become previews
                          type: array
                          items:
                            type: string
                        limit:
                          description: maximum number of simultaneous previews
                          type: integer
                          minimum: 1
                          maximum: 10000
                    interval:
                      description: how often the forge is polled for change requests
                      type: string
                    provider:
                      description: the forge whose change requests become previews
                      type: string
                      enum:
                        - github
                        - gitlab
                    repo:
                      description: HTTP(S) URL of the source repository whose change requests become previews; not delivery.git.repo
                      type: string
                    secretRef:
                      description: name of the Secret holding forge credentials; never a token. Omit it to have kelson materialize one from the git connection covering previews.repo (ADR-0033)
                      type: string
                    skip:
                      type: object
                      properties:
                        labels:
                          description: pause preview updates while one of these labels is present; a ! prefix inverts the test
                          type: array
                          items:
                            type: string
                project:
                  type: string
                routing:
                  type: object
                  properties:
                    domainSuffix:
                      type: string
                    gatewayClass:
                      type: string
                    tls:
                      type: boolean
                secrets:
                  type: object
                  required:
                    - backend
                  properties:
                    ageKeySecret:
                      description: sops only; name of the Secret holding the age identity — created by the operator in the Kustomization's namespace
                      type: string
                    ageRecipients:
                      description: sops only; age public keys (age1…) that encrypted secrets are readable by — required for backend sops
                      type: array
                      items:
                        type: string
                    backend:
                      type: string
                      enum:
                        - cluster
                        - externalSecrets
                        - sops
                    refreshInterval:
                      description: externalSecrets only; how often the value is re-read from the backing store; a positive Go duration such as 30s or 15m or 1h
                      type: string
                    store:
                      description: externalSecrets only; name of a SecretStore in this namespace or a ClusterSecretStore — optional when the cluster offers exactly one
                      type: string
            status:
              description: 'EnvironmentStatus is what the controller observed about this Environment: the validation record a Project also has, plus the delivery record.'
              type: object
              properties:
                conditions:
                  description: the observed conditions, with Ready as the summary
                  type: array
                  items:
                    type: object
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        description: when the condition last changed from one status to another
                        type: string
                        format: date-time
                      message:
                        description: a human-readable message about the transition, which may be empty
                        type: string
                        maxLength: 32768
                      observedGeneration:
                        description: the .metadata.generation the condition was set from
                        type: integer
                        format: int64
                        minimum: 0
                      reason:
                        description: a programmatic identifier for the condition's last transition
                        type: string
                        minLength: 1
                        maxLength: 1024
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      status:
                        description: True, False or Unknown
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      type:
                        description: the condition's type, in CamelCase; Ready is kelson's summary condition
                        type: string
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys:
                    - type
                history:
                  description: the bounded mirror of published revisions, newest first. The record is the registry's tag list; this is a window onto it for humans and for the API (ADR-0028 decision 4).
                  type: array
                  maxItems: 20
                  items:
                    type: object
                    required:
                      - revision
                    properties:
                      componentImages:
                        description: the container images this revision resolved to, each named with the component that resolved it. A promotion reads this to know which component an image belongs to rather than deriving it (ADR-0016 decision 2).
                        type: array
                        items:
                          type: object
                          required:
                            - component
                            - image
                          properties:
                            component:
                              description: the component's name as the resolved spec spelled it
                              type: string
                            image:
                              description: the image reference this revision resolved that component to
                              type: string
                      digest:
                        description: the artifact's OCI digest — what was put there, as opposed to where
                        type: string
                      images:
                        description: 'DEPRECATED: the container images this revision resolved to, in component order, with no way to tell which component each belongs to. It is a flat mirror of componentImages, kept for one release for readers written against the older shape.'
                        type: array
                        items:
                          type: string
                      outcome:
                        description: 'the delivery phase this revision reached: Proposed, Committed, Reconciling, Applied, Healthy, Degraded or Rejected'
                        type: string
                      revision:
                        description: 'the artifact tag: <generation>-<spec-hash-short>'
                        type: string
                      specHash:
                        description: hash of the resolved spec this revision was rendered from
                        type: string
                      timestamp:
                        description: when the entry was recorded
                        type: string
                        format: date-time
                observedGeneration:
                  description: the .metadata.generation this status describes. A status whose observedGeneration trails the object's generation has not caught up yet.
                  type: integer
                  format: int64
                phase:
                  description: 'the delivery state-machine phase: Proposed, Committed, Reconciling, Applied, Healthy, Degraded or Rejected'
                  type: string
                revision:
                  description: the settled revision — the artifact tag the OCIRepository is pinned to
                  type: string
                rollbackGeneration:
                  description: the .metadata.generation the rollback was applied at. A generation past this one means the spec was edited since, which resumes normal publishing (ADR-0028 decision 5).
                  type: integer
                  format: int64
                rollbackRevision:
                  description: the kelson.dev/rollback-to value the controller has acted on
                  type: string
                validationErrors:
                  description: 'what validate.go said about this document. An invalid spec is a status and not a rejection: nothing renders, nothing is published, and the previous revision keeps serving.'
                  type: array
                  items:
                    type: object
                    required:
                      - code
                      - message
                    properties:
                      code:
                        description: the stable, machine-actionable code, e.g. schema/unknown-field
                        type: string
                      column:
                        description: 1-based column in the authored YAML, when the source was available
                        type: integer
                      docsUrl:
                        description: documentation page for this code
                        type: string
                      field:
                        description: JSONPath-like location within the document, e.g. $.spec.components[2].port
                        type: string
                      line:
                        description: 1-based line in the authored YAML, when the source was available
                        type: integer
                      message:
                        description: what is wrong
                        type: string
                      remediation:
                        description: what to do about it, stated as an action
                        type: string
                      resource:
                        description: the document the error is about, e.g. Environment/production
                        type: string
          required:
            - spec
