apiVersion: kelson.dev/v1alpha1
kind: Environment
metadata:
  name: production

spec:
  project: streaming
  namespace: streaming

  routing:
    domainSuffix: acme.com
    gatewayClass: envoy

  # Agent guardrails for this environment (ADR-0025). Humans are unaffected.
  policy:
    agents: propose-only
    protect: [transcoder]

  components:
    - name: transcoder
      replicas: { min: 2, max: 8 }
