# Code generated by internal/schemagen. DO NOT EDIT.
#
# The openAPIV3Schema below is reflected from the Go types in internal/model —
# the same reflection that produces schema/*.json — so the CRD and the published
# JSON Schema cannot disagree about what a valid kelson spec is (ADR-0027
# decision 4). Regenerate with:
#
#	go generate ./internal/model
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: gitconnections.kelson.dev
  labels:
    app.kubernetes.io/name: kelson
    app.kubernetes.io/component: crd
spec:
  group: kelson.dev
  names:
    kind: GitConnection
    listKind: GitConnectionList
    plural: gitconnections
    singular: gitconnection
    shortNames:
      - kconn
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Provider
          type: string
          jsonPath: .spec.provider
          description: the forge adapter this connection uses
        - name: Host
          type: string
          jsonPath: .spec.host
          description: the forge base URL; empty means the provider's default
        - name: Account
          type: string
          jsonPath: .status.account
          description: who the credential acts as, as the provider reports it
        - name: Repos
          type: integer
          jsonPath: .status.repositories
          description: how many repositories the credential can see
        - name: Ready
          type: string
          jsonPath: .status.conditions[?(@.type=="Ready")].status
          description: whether the document validated and the credential resolved
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
      schema:
        openAPIV3Schema:
          description: 'GitConnection is a forge kelson can talk to and the Secret it talks with: which provider and host, which credential kind, and who owns it. It carries identifiers and Secret names and never a credential value (ADR-0033).'
          type: object
          properties:
            apiVersion:
              description: APIVersion defines the versioned schema of this representation of an object.
              type: string
            kind:
              description: Kind is a string value representing the REST resource this object represents.
              type: string
            metadata:
              type: object
            spec:
              type: object
              required:
                - provider
                - auth
              properties:
                auth:
                  type: object
                  properties:
                    githubApp:
                      description: provider github only; the per-instance GitHub App this connection acts as
                      type: object
                      required:
                        - appID
                        - secretRef
                      properties:
                        appID:
                          description: the GitHub App's numeric ID as the manifest flow reports it
                          type: integer
                          minimum: 1
                        installationID:
                          description: the installation this app acts as; 0 is the valid pre-install state before the installation webhook arrives
                          type: integer
                          minimum: 0
                        secretRef:
                          description: name of the Secret holding the app private key and webhook secret; never a value
                          type: string
                          minLength: 1
                    token:
                      description: a personal or project access token held in a Secret; the fallback every forge answers
                      type: object
                      required:
                        - secretRef
                      properties:
                        secretRef:
                          description: name of the Secret holding the token; never a value
                          type: string
                          minLength: 1
                  x-kubernetes-validations:
                    - rule: has(self.githubApp) != has(self.token)
                      message: 'set exactly one of auth.githubApp or auth.token: they are different credential kinds and not two spellings of one (schema/mutually-exclusive)'
                host:
                  description: forge base URL such as https://github.com or https://git.acme.internal; defaults to https://github.com for provider github
                  type: string
                  format: uri
                owner:
                  type: object
                  required:
                    - kind
                  properties:
                    kind:
                      description: who may edit this document; only instance is enforced today
                      type: string
                      enum:
                        - instance
                        - user
                        - team
                    name:
                      description: the owning principal; required when kind is user or team and refused when it is instance
                      type: string
                  x-kubernetes-validations:
                    - rule: '!has(self.kind) || self.kind == ''instance'' || (has(self.name) && self.name != '''')'
                      message: an owner whose kind is user or team names the principal it belongs to (schema/missing-required)
                    - rule: '!has(self.kind) || self.kind != ''instance'' || !has(self.name) || self.name == '''''
                      message: 'an instance-owned connection names no principal: remove owner.name, or set owner.kind to user or team (schema/mutually-exclusive)'
                provider:
                  description: the forge this connection speaks to; the enum grows one value per adapter
                  type: string
                  enum:
                    - github
                    - generic
              x-kubernetes-validations:
                - rule: '!has(self.auth) || !has(self.auth.githubApp) || (has(self.provider) && self.provider == ''github'')'
                  message: 'auth.githubApp needs provider github: the app-manifest flow and installation tokens are GitHub''s and no other adapter can mint one (semantic/auth-provider-mismatch)'
            status:
              description: 'GitConnectionStatus is what the control plane observed about this GitConnection: whether the document is usable, and what the forge said when kelson used it.'
              type: object
              properties:
                account:
                  description: 'who the credential acts as, as the provider reports it: the organization or user the app is installed on, or the account a token belongs to'
                  type: string
                conditions:
                  description: the observed conditions, with Ready as the summary and Reachable as the forge's own answer
                  type: array
                  items:
                    type: object
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    properties:
                      lastTransitionTime:
                        description: when the condition last changed from one status to another
                        type: string
                        format: date-time
                      message:
                        description: a human-readable message about the transition, which may be empty
                        type: string
                        maxLength: 32768
                      observedGeneration:
                        description: the .metadata.generation the condition was set from
                        type: integer
                        format: int64
                        minimum: 0
                      reason:
                        description: a programmatic identifier for the condition's last transition
                        type: string
                        minLength: 1
                        maxLength: 1024
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      status:
                        description: True, False or Unknown
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      type:
                        description: the condition's type, in CamelCase; Ready is kelson's summary condition
                        type: string
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                  x-kubernetes-list-type: map
                  x-kubernetes-list-map-keys:
                    - type
                observedGeneration:
                  description: the .metadata.generation this status describes. A status whose observedGeneration trails the object's generation has not caught up yet.
                  type: integer
                  format: int64
                repositories:
                  description: 'how many repositories this credential can see, as the provider reports it. A scope readout rather than a count: an installation that should cover forty repositories and reports one is a picked-the-wrong-repository mistake.'
                  type: integer
                  format: int32
                validationErrors:
                  description: 'what validate.go said about this document. An invalid spec is a status and not a rejection: nothing renders, nothing is published, and the previous revision keeps serving.'
                  type: array
                  items:
                    type: object
                    required:
                      - code
                      - message
                    properties:
                      code:
                        description: the stable, machine-actionable code, e.g. schema/unknown-field
                        type: string
                      column:
                        description: 1-based column in the authored YAML, when the source was available
                        type: integer
                      docsUrl:
                        description: documentation page for this code
                        type: string
                      field:
                        description: JSONPath-like location within the document, e.g. $.spec.components[2].port
                        type: string
                      line:
                        description: 1-based line in the authored YAML, when the source was available
                        type: integer
                      message:
                        description: what is wrong
                        type: string
                      remediation:
                        description: what to do about it, stated as an action
                        type: string
                      resource:
                        description: the document the error is about, e.g. Environment/production
                        type: string
          required:
            - spec
